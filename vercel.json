{
  "version": 2,
  "buildCommand": "pnpm run build:frontend",
  "outputDirectory": "artifacts/narayani/dist/public",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/index?path=/:path*" },
    { "source": "/sitemap.xml", "destination": "/api/index?path=/sitemap.xml" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ],
  "functions": {
    "api/index.ts": {
      "maxDuration": 30
    }
  }
}
